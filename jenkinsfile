pipeline {
   agent any

   stages {
      stage('build') {
         steps {
            //mvn clean install
            echo 'Hello World build'
         }
      }
      stage('test') {
         steps {
            //mvn test
            echo 'Hello World test'
         }
      }
      stage('deploy dev') {
         steps {
            echo 'Hello World dev'
         }
      }
      stage('deploy test') {
         steps {
            echo 'Hello World test'
         }
      }
   }
}
